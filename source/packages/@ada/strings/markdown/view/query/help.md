The Query workbench allows you to run SQL-like queries on Data Products that have been successfully imported.

You can:

- Save queries or share them within a domain.
- Export query results in a .CSV format.
