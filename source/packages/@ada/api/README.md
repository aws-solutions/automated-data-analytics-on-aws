# @ada/api package

This package contains all the "api" peripherial, such as generated sdk specs, sdk clients, docs, and utils;
it does **not** contain the source of api definition which comes from CDK constructs in the infra.
